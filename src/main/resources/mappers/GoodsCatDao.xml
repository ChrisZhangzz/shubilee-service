<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shubilee.dao.GoodsCatDao">

    <select id="selectCartidByGid" parameterType="int" resultType="String">
    SELECT cat_id FROM xysc_goods_cat where goods_id = #{goods_id}
  </select>
</mapper>
